# Integration Tests

Place Node-based tests of interactions between services and mocked Foundry adapters here. These tests may compose several production modules but must not require Foundry globals, a browser client, a live world, or network access.

Live Foundry compatibility procedures and results belong under `docs/test-runs/`.
