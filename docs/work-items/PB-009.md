# PB-009 — Add the automated test foundation

Status: `DONE`

Completed: 2026-08-14

## Implemented outcome

Added a dependency-free automated test foundation using the Node.js built-in test runner. The default test command discovers Node-safe tests, and the repository check now runs tests before syntax and manifest validation.

## Files changed

- `package.json`: Added `npm test` and made `npm run check` execute the test suite first.
- `tests/README.md`: Defines automated and live-Foundry test boundaries.
- `tests/helpers/project-files.mjs`: Provides repository-relative JSON and file helpers.
- `tests/unit/module-manifest.test.mjs`: Tests module identity, package alignment, file references, and release URLs.
- `tests/integration/README.md`: Reserves mocked service/adapter integration tests without a live Foundry dependency.
- `tests/fixtures/README.md`: Defines minimal synthetic-fixture and data-rights requirements.

## Automated verification

- `npm test`: 3 passed, 0 failed.
- `npm run check`: 3 passed, 0 failed; entry-script syntax and manifest validation passed.
- A temporary intentional assertion failure run with `node --test` exited with status 1.
- The intentional failure file was removed after verification and is not part of the repository.

## Manual verification

- Confirmed unit, mocked integration, fixture, and live Foundry/manual test responsibilities are documented separately.
- Confirmed the test suite uses no personal world data or third-party content.

## Known limitations and follow-up

- Foundry runtime behavior cannot be validated by the Node-only suite. PB-003 through PB-008 cover those compatibility checks.
