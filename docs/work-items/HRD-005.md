# HRD-005 — Final Foundry Compatibility Matrix

## Implemented outcome

Installed the 0.5.0 release-candidate archive into new disposable Foundry data
roots, created new `hyp3e` worlds, and completed the full GM/Player smoke and
distribution matrix on every supported combination.

| Foundry | `hyp3e` | Result |
| --- | --- | --- |
| 13.351 | 4.0.3 | Pass |
| 14.365 | 4.1.0 | Pass |

The complete evidence is recorded in
`docs/test-runs/2026-08-15-hrd005-clean-matrix.md`.

## Files changed

- `tests/foundry/diagnostics/diagnostics.mjs`
- `tests/foundry/diagnostics/m5-distributions.mjs`
- `docs/test-runs/2026-08-15-hrd005-clean-matrix.md`
- `docs/compatibility-notes.md`
- `IMPLEMENTATION-PLAN.md`

## Automated checks

- Complete `npm run check`: 234 tests passed; production and diagnostic syntax
  checks and manifest validation passed.
- Release-candidate ZIP contents, size, SHA-256, and independent extraction
  were recorded.

## Manual checks

- Foundry 13.351 / `hyp3e` 4.0.3: 37 GM/Player diagnostic groups completed
  with empty error arrays; XP, coins, and wages passed; cleanup restored exact
  state.
- Foundry 14.365 / `hyp3e` 4.1.0: the same complete matrix passed.
- Both environments used fresh worlds and module directories extracted from
  the release-candidate ZIP, not working-tree junctions.
- Final browser-log review found only deliberate negative-path notices,
  Foundry's viewport warning, and the known Foundry 13 `hyp3e` ContextMenu
  deprecation.

## Known limitations and follow-up

- The in-app browser viewport was 1280 by 720. Responsive behavior at that
  constrained size is covered, while Foundry itself warns that 768 pixels of
  effective height is preferred.
- `hyp3e` 4.0.3 emits a ContextMenu migration warning under Foundry 13. This is
  upstream system code and does not affect module behavior.
- PKG-001 will repeat installation using final 1.0.0 metadata and verify the
  release workflow. Public-URL installation remains gated on explicit release
  authorization.

No design-document change was required.
