# FND-001 — Establish module constants and logging

Status: `DONE`

Completed: 2026-08-14

## Implemented outcome

Added stable module, environment, setting, flag, hook, template, save, coin,
and CSS identifiers plus a consistently prefixed logging helper.

## Files changed

- `module/core/constants.mjs`: Defines the shared foundation contract.
- `module/core/logger.mjs`: Provides scoped debug, info, warning, and error
  output.
- `tests/unit/constants.test.mjs`: Locks the public keys and logger prefix.

## Verification

- All automated tests and repository validation passed with `npm run check`.
- Foundry 13.351 and 14.365 loaded the production entry point and published the
  expected module API.

## Known limitations and follow-up

- Feature-specific hooks and keys will be added with their owning work items.
