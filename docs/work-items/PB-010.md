# PB-010 — Remove HYPERBOREA from the public project identity

Status: `DONE`

Completed: 2026-08-14

## Implemented outcome

Renamed the module's public identity to Hyp3e Utilities with package and Foundry module ID `hyp3e-utilities`. Aligned the GitHub repository, local Git remote, release URLs, install artifact, module-owned filenames, namespace guidance, and local repository directory.

## Files changed

- `module.json`: Uses the `hyp3e-utilities` identity and `DT357/hyp3e-utilities` URLs.
- `package.json`: Uses package name `hyp3e-utilities`.
- `.github/workflows/validate-and-release.yml`: Produces `hyp3e-utilities.zip` and Hyp3e Utilities release titles.
- `HYP3E-UTILITIES-DESIGN.md`: Uses the new title, ID, filenames, CSS prefix, and flag namespace.
- `IMPLEMENTATION-PLAN.md`: Records the completed rename and updated repository connection.
- `README.md` and `THIRD_PARTY_NOTICES.md`: Use the new public identity.

## Repository operations

- Changed `origin` to `https://github.com/DT357/hyp3e-utilities.git`.
- Renamed the nested local Git repository directory from `hyperborea-utilities` to `hyp3e-utilities`.
- Did not commit or push repository contents.

## Automated verification

- Run `npm run check` from the renamed directory.
- Confirm manifest entry script, stylesheet, and license paths resolve.
- Confirm all 66 implementation work-item IDs remain unique.
- Confirm stale-name searches return no old package ID except explanatory historical text, if any.
- Verify the new `origin` is reachable.

## Known limitation

- The outer workspace directory retains its existing descriptive name. It is not the module's Git repository or distributed package directory.
