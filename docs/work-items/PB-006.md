# PB-006 — Verify managed treasury Actor behavior

Status: `DONE`

Completed: 2026-08-14

## Implemented outcome

Verified that a flagged world `treasure` Actor is a viable party-treasury
backend and that lookup must be flag-based and rename-tolerant.

## Files changed

- `tests/foundry/diagnostics/diagnostics.mjs`: Added treasury creation, flag,
  ownership, rename, deletion, recreation, duplicate-detection, and cleanup
  checks.
- `HYP3E-UTILITIES-DESIGN.md`: Recorded the runtime verification.
- `docs/compatibility-notes.md`: Recorded the compatibility contract.

## Automated verification

- `npm run check` syntax-checks the diagnostic module.

## Manual verification

- `hyp3e` 4.1.0 accepted a world Actor of type `treasure`.
- The module flag and GM ownership persisted.
- Flag lookup survived rename, detected deletion, supported recreation, and
  returned both candidates when duplicates existed.

## Known limitations and follow-up

- TRY-001 must implement the production folder, binding, recovery prompt, and
  duplicate-selection UI. The diagnostic never deletes an extra treasury
  silently.
